# 결정적 script 노드 표준 픽스처 — node test·E2E 스모크용
name: greet
kind: script
description: 이름을 받아 인사말 JSON을 출력
inputs:
  name: {type: string, required: true}
outputs:
  greeting: {type: string}
run:
  command: "jq -c '{greeting: (\"hello, \" + .name)}'"
  input: stdin-json
  output: stdout-json

# repair

autopilot 의 버그 전용 SPEC 작성 스킬 — `spec` 의 형제. 버그·증상·실패 테스트 설명에서 정적 분석으로 근본 원인을 진단하고 그 수정용 자기완결 SPEC 문서를 산출한다. 코드·머지·외부 상태는 만들지 않으며, `spec` 의 SPEC 작성 자산을 단일 출처로 재사용한다.

상세는 `SKILL.md` 와 `references/` 참조.
